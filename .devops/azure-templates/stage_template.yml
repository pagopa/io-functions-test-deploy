
parameters:
  - name: 'deploySDK'
    type: boolean
    default: False

  - name: 'dependsOn'
    type: object
    default: []

  - name: 'isBranchMaster'
    default:  $[ eq(variables['Build.SourceBranch'], 'refs/heads/master') ]

  - name: 'variableValue'
    type: string

stages:

- stage: Deploy_SDK
  dependsOn: ${{parameters.dependsOn}}
  condition: eq(variables.deploySDK, true)
  jobs:
  - job: B1
    steps:
      - script: echo Hello Stage B!
      - script: echo $(deploySDK) $(isBranchMaster)

- stage: Deploy_SDK_2
  dependsOn: []

  variables: 
    ${{ if eq(parameters.variableValue, '') }}:
      myVariable: ''
    ${{ if ne(parameters.variableValue, '') }}:
      myVariable: 'C''è un valore'

  jobs:
  - job: B2
    steps:
      - script: echo Hello ${{ variables.myVariable }}! -> ${{ parameters.variableValue }} !!